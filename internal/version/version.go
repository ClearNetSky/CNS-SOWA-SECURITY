// Package version holds the single source of truth for the
// application version, shared by the CLI banner, the API and the UI.
package version

// Version is the current S.O.W.A Security release.
const Version = "1.5.0"
